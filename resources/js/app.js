import './bootstrap';

// Alpine.js completely removed from app.js
// Filament 3.x bundles its own Alpine.js and handles all admin panel JS
// Frontend pages that need Alpine should load it separately via CDN or dedicated script
